@model IEnumerable<HastaneUygulamasi.Models.Katlar>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Katlar</h1>

<p>
    <a asp-action="Create">Yeni Kat Giriş</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.KatNumarası)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.KatNumarası)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id">Güncelle</a> |
                <a asp-action="Delete" asp-route-id="@item.id">Sil</a>
            </td>
        </tr>
}
    </tbody>
</table>
